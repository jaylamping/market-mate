Verified head 4514fb0. Existing archive acceptance, cargo test --locked, cargo fmt --check, frontend npm test (39 passed), npm run typecheck, npm run build and git diff --check passed. Read-only application and acceptance reviewers found no material issues. SQL review is not applicable: this diff changes one frontend file only. The archive icon is a sibling native button, supports keyboard activation, and intercepts clicks while disabled so they do not activate the card underneath.
