Experiment tickets now start a Setup agent automatically. Setup checks the pinned research, can clarify once with the original researcher, and waits for explicitly attached registered data. A validated package is preregistered and handed to the separate Experiment role for bounded local momentum diagnostic execution. Results and blockers reach Incubator through SSE.

The worker uses durable dispatch intents, atomic worker exclusion, immediate PostgreSQL notifications and a 60-second recovery scan. Interrupted model requests are never replayed; deterministic local execution can recover. Role-specific model preferences preserve provider order and the existing zero-spend budget. These diagnostics grant no trading authority or research qualification.

Validation: isolated experiment and upstream evaluation acceptance scripts; cargo test --locked; cargo fmt --check; frontend npm test (39 tests), npm run typecheck and npm run build; git diff --check. Experiment acceptance covers two-worker pickup, original-model clarification, attachment idempotency, preregistration, superseded inputs, SSE results and restart behavior. Parallel repository-required review follows before merge.
