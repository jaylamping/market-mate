# AGENTS.md

## Agent skills

### Issue tracker

GitHub Issues on jaylamping/market-mate via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### WU loop

One work unit lands as one PR through four phases: implement, review, resolve, merge. See `docs/agents/wu-loop.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.